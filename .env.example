PORT=3000
NODE_ENV=development

# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=prorag
MYSQL_PASSWORD=prorag_secret
MYSQL_DATABASE=prorag
MYSQL_ROOT_PASSWORD=root_secret

# Qdrant
QDRANT_URL=http://localhost:6333

# OpenAI
OPENAI_API_KEY=
EMBEDDING_MODEL=text-embedding-3-large
LLM_MODEL=gpt-5.2

# Jina
JINA_API_KEY=
RERANKER_PROVIDER=jina

# Chunking
DEFAULT_CHUNK_SIZE=512
DEFAULT_CHUNK_OVERLAP=50
DEFAULT_CHUNK_STRATEGY=recursive
SEMANTIC_SIMILARITY_THRESHOLD=0.85

# Retrieval
DEFAULT_RETRIEVAL_TOP_K=20
DEFAULT_RERANKER_TOP_N=5
DEFAULT_TEMPERATURE=0.1
DEFAULT_SEARCH_MODE=hybrid
DEFAULT_QUERY_STRATEGY=direct
DEFAULT_VECTOR_WEIGHT=0.7

# BM25 / RRF
BM25_K1=1.2
BM25_B=0.75
RRF_K=60

# Pricing (USD per 1K tokens)
OPENAI_EMBEDDING_PRICE_PER_1K=0.00013
OPENAI_PROMPT_PRICE_PER_1K=0.005
OPENAI_COMPLETION_PRICE_PER_1K=0.015
JINA_RERANKER_PRICE_PER_1K=0.00002

# Limits
MAX_FILE_SIZE_MB=50
MAX_FILES_PER_UPLOAD=10
LLM_MAX_TOKENS=4096
